<IfModule mod_rewrite.c>
    RewriteEngine On

    # If request is already for /public, skip
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirect everything else to /public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>